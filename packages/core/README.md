# packages/core — OBSIDIAN CORE

> **Status:** Placeholder — Phase 0 foundation. No code yet.

Shared platform kernel: AI orchestrator entrypoint, permission engine (Levels 1–4), event bus contracts, memory interfaces, audit-log service, tenant context. Every vertical depends on core; core depends on no vertical.

See `/docs/ARCHITECTURE.md` for how this module fits the modular monolith, and `/docs/AI_HANDOFF.md` for the current build state.
