# packages/ai — AI Orchestration

> **Status:** Placeholder — Phase 0 foundation. No code yet.

Model routing, tool-calling framework, tool schema registry, prompt/context assembly, structured-action proposals. Converts natural language into validated tool calls. No direct DB access — goes through tools.

See `/docs/ARCHITECTURE.md` for how this module fits the modular monolith, and `/docs/AI_HANDOFF.md` for the current build state.
