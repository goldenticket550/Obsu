# packages/database — Data Layer

> **Status:** Placeholder — Phase 0 foundation. No code yet.

Postgres schema, migrations, Row-Level Security policies, typed query layer, repository interfaces. Owns the shared data model; verticals extend it via their own tables + RLS.

See `/docs/ARCHITECTURE.md` for how this module fits the modular monolith, and `/docs/AI_HANDOFF.md` for the current build state.
